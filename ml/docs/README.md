# ML Documentation

The old API and infrastructure notes were removed during the frontend migration.
Current product workflow lives in the SvelteKit app under `frontend/`, with data
models and verification functions in `frontend/amplify/`.

Use this directory only for new Numerai training pipeline notes that match the
Amplify-backed product architecture.
